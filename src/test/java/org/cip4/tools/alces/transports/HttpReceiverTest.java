/*
 * Created on Apr 23, 2005
 */
package org.cip4.tools.alces.transports;

import org.apache.commons.logging.Log;
import org.apache.commons.logging.LogFactory;
import org.cip4.tools.alces.transport.HttpReceiver;

import junit.framework.TestCase;

/**
 * @author Claes Buckwalter (clabu@itn.liu.se)
 */
public class HttpReceiverTest extends TestCase {

    private static Log log = LogFactory.getLog(HttpReceiverTest.class);
    
    public void testHttpReceiver() throws Exception {
        /*
        HttpReceiver http = new HttpReceiver(null);
        http.startServer(9090);
        assertTrue(http.isStarted());
        Thread.sleep(50000);
        http.stopServer();
        assertFalse(http.isStarted());
        */
    }
    
}
