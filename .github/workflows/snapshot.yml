name: Snapshot

on:
  workflow_dispatch:
  push:
    branches: [ main, extract-workflows ]
env:
  tool: Alces
  confluence-label: alces
  confluence-content-id: -1
jobs:
  call-tool-snapshot:
    uses: cip4/.github/.github/workflows/tool-snapshot.yml@workflow-tool-snapshot
    with:
      tool: Alces
      confluence-label: alces
      confluence-content-id: 1457554021
      apple-bundle-id: org.cip4.alces
