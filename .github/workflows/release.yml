name: Release

on:
  push:
    tags:
      - '*'
jobs:
  call-tool-release:
    name: 🏭
    uses: cip4/.github/.github/workflows/tool-release.yml@workflow-tool-snapshot
    with:
      tool: Alces
      confluence-label: alces
      # TODO: replace with correct ID (1181122563) once test is completed
      confluence-content-id: 1377338047
      apple-bundle-id: org.cip4.alces
    secrets:
      APPLE_PASSWORD: ${{ secrets.APPLE_PASSWORD }}
      APPLE_CERTIFICATE: ${{ secrets.APPLE_CERTIFICATE }}
      APPLE_CERTIFICATE_PASSWORD: ${{ secrets.APPLE_CERTIFICATE_PASSWORD }}
      ATLASSIAN_USER: ${{ secrets.ATLASSIAN_USER }}
      ATLASSIAN_TOKEN: ${{ secrets.ATLASSIAN_TOKEN }}
